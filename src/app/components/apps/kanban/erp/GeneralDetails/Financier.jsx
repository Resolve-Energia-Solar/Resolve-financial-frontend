import { Box } from "@mui/material";
import Header from "./Header";

export default function Financier({ data }) {
    return <div>

        <Header data={data} />
        <Box>
            
        </Box>
    </div>
}